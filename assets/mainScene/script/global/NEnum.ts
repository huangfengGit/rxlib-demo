export enum E_SysType {
    NONE = 0,
    PC_BROWSER = 1,
    ANDROID_BROWSER = 2,
    IOS_BROWSER = 3,
}

export enum E_BuffType {
    NONE = 0,
    GLOBAL = 1,
    BATTLE = 2,
}

export enum E_StageType {
    NONE = 0,
    LOADING,
    LOBBY,
    BATTLE,
}

export enum E_Battle_Type {
    NONE = 0,
    PVP,
    PVE,
    SHOW_PVP,
    SHOW_PVE,
    ECSTEST,
}
